throw new Error(
  'verify-live-auth.mjs is intentionally disabled. It must never authenticate against, query, or create users in a hosted Supabase project. Use the local UAT seed and test scripts only.',
);
